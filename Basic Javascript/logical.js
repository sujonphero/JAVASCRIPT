// this is logical / relational operetor.

// &&   and
// ||   or
// !    not


// && and / when total true return true and when one false return false.
console.log(10 < 20 && 20 > 21);

// || or / when total true result true then total false return false.
console.log(10 < 20 || 20 > 2);

// ! not / it mean oppsite.
console.log(!true);